{
  "name": "decision-map-extension",
  "version": "1.1.0",
  "private": true,
  "description": "Tableau Dashboard Extension that renders a decision tree from staged state columns; empty/skipped stages are hidden while keeping the stage columns aligned.",
  "scripts": {
    "start": "python3 serve.py",
    "serve": "python3 serve.py"
  }
}
